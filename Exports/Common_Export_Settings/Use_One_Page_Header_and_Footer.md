## Use One Page Header and Footer

When exporting to Excel, the whole report is converted into one table. Headers and footers of a page break this table. This property leaves only the first header and the last header of a page. All other headers and footers are removed. If it is necessary to save the initial view of a document then it is necessary to set this property to **false**. For each type of export, its own property is used.
