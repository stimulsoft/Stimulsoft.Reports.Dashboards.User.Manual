## DBF

**DBF** (DataBase File) is the format to store data and it is used as the standard way to store and pass information. The DBF file consists of a header section for describing the structure of the data in the file. There are several variations on the .dbf file structure.


* **Note:** Only data can be exported to the DBF format, in other words only the components that are placed on data bands.
